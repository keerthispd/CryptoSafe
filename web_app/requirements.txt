Flask==3.1.0
cryptography==45.0.4
webauthn==2.7.0
bcrypt==4.0.1
pyjwt==2.8.0    
gunicorn==23.0.0
Werkzeug
segno==1.6.6
# Optional: SQLCipher Python bindings. Not available on all platforms (Windows/Python combos).
# Install only if your runtime supports SQLCipher C library (see web_app/README.md "SQLCipher notes").
# sqlcipher3-binary